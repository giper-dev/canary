namespace $.$$ {
	export class $hd_canary_app_profile extends $.$hd_canary_app_profile {

		@ $mol_mem
		home() {
			// берем свой уголок
			const home = this.$.$hyoo_crus_glob.home( $hd_canary_app_home )
			home.land().give(null, $hyoo_crus_rank_rule)
			return home
		}

		@$mol_mem
		profile_name(next: any){
			return this.home().title(next)
		}
	}
}